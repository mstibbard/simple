# simple

Alchemy v2 tutorial Part 1 project: a Cloudflare-backed stack with one R2 bucket.

## Setup

Dependencies are already installed. To reinstall them:

```bash
bun install
```

## Plan or deploy

```bash
bun run plan
bun run deploy
```

The first deploy will prompt for Cloudflare credentials and ask before creating the bucket.
